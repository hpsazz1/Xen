# r64 三场景实机门禁完成

## 背景

r64以35 ms命令响应速率不确定度尾部通过九点static负样本门禁后，仍需用同一程序身份确认jump与当前最快reverse不会被额外15 ms尾部错误压制。既有r63原始数据在35 ms候选下已复放覆盖jump 7/7、reverse 6/6，本轮目标只是在真实r64运行时核对身份、诊断契约和正样本启用，不继续搜索参数。

## 数据与身份

隔离分析以下文件：

- `C:\Users\16143\Desktop\Xen\DML\ndi\static.csv`；
- `C:\Users\16143\Desktop\Xen\DML\ndi\horizontal_jump.csv`；
- `C:\Users\16143\Desktop\Xen\DML\ndi\horizontal_reverse.csv`。

三份CSV共6496帧，身份统一为`DML / 6218a875ae46 / 20260719T141332Z / r64`。逐帧配置均为shadow、20/20 ms有限响应、`maneuver_gated_ca + 8000/12/120`、不确定度增益1.25、尾部35 ms、轨迹整形关闭、Profile标定关闭，不存在混合后端、混合提交、`-dirty`或旧控制器数据。

## 正负样本覆盖

static共2421帧和9个完整运行段，运行态机动启用为0；295个启用帧全部位于松键人工换位的暂停态，负样本继续通过。

jump共2212帧，其中运行态2169帧、持续23.18秒。运行态机动启用75帧，形成6个独立连续启用区间，门控证据峰值14.90°/s；单次长按内部已多次越过和退出物理门槛，不是单帧偶然命中。

reverse共1863帧，其中运行态1812帧、持续18.92秒。运行态机动启用948帧，形成44个独立连续启用区间，门控证据峰值97.62°/s；200个启用帧与legacy settled重合，属于真实高速反转正样本，不构成static误触。

## 正式分析结果

正式分析器对三个文件分别给出PASS，overall也为PASS。总计1023个运行态机动启用帧；模式、构建身份、时序、估计有限性、暂停零命令、暂停连续观测、门控参数、选择序列、20/20 ms响应、1.25增益、35 ms尾部和逐轴不确定度乘积均为0违例。

虽然jump和reverse各只有一个连续按键段，但段内分别包含6次和44次独立启用，并且既有原始数据的反事实复放已经验证多个独立运行段全部保留覆盖。因此继续要求相同程序和配置重复采集的边际价值不足，不再占用主线。

## 结论与后续

r64完成“static不误触、jump/reverse真实机动不漏检”的最小实机闭环，35 ms尾部冻结，不再调整增益、响应中心/宽度、门槛或保持时间。P0-6F关闭。

本结论仍不代表新控制链可以切换active。P0-5跨域回放仍有static、jump和reverse失败域，运行时继续`HOLD_SHADOW`，正式设备输出继续由legacy产生。下一主线应回到P0-5跨域失败域与active晋级条件；分轴主动Profile标定保持P1，不自动应用本轮机器测量结果。
