# r58普通场景安全回归与legacy冻结

## 数据身份与门禁范围

2026-07-16复核`C:\Users\16143\Desktop\Xen\DML\ndi`中新上传的`horizontal_left.csv`、`horizontal_right.csv`、`horizontal_reverse.csv`和`static.csv`。四份数据统一为`DML / 1fd263e9f7e5 / r58 / legacy`，每帧`TrackerStaleTimeoutMs=100`且`ConditionalSpeedBudgetActive=0`。配置保持320裁剪、响应120 ms、基础与条件速度上限均3200 counts/s、积分500 ms和预测50/50/1.0。

本轮用于验证r58检测发布时钟修复没有给普通场景引入成熟方向硬复位、错误输出换向或静止停发回归，不再搜索legacy局部参数。

## 单向移动

left/right各形成1个有效试次，真实水平误差P95分别为13和12.5 px，框内率均为100%。两侧预测生效率均为100%，预测中断、预测换向和最终设备输出换向全部为0；同向输出步进P95均为3 counts，限速率为0。left有1个30.07 ms发布间隔但方向锁没有下降，right最大间隔20.67 ms。单向连续性和输出稳定性通过。

## 往返与静止

reverse形成1个有效试次，真实水平误差P95为19.5 px、框内率92.8%；12次识别反转全部恢复，平均恢复58.9 ms、P95为83 ms，输出换向最大5 counts且限速率为0。该文件包含76个25 ms以上发布间隔，最大49.5 ms。仅2次方向锁下降位于28.32和31.72 ms间隔后，两处误差只有1.2和0.2 px，均明确命中振荡与运动证据抑制；观测速度仍保留65.7和109.3 px/s，属于反转中央走廊的预期安全门控，不是状态硬清空。

static按松开间隔形成9个有效试次，真实水平误差P95均值2.61 px、最差6.5 px；九段热身后的请求、积分和最终设备输出均为0，预测生效率0、运动证据抑制100%、X轴稳定锁存100%，没有输出换向。段间最大1.65秒空档属于主动松开分段，未产生陈旧命令。

## 结论

r58/3200通过普通场景安全回归：left/right连续且不换向，reverse全部可靠恢复，static最终完全停发；25～49.5 ms检测发布抖动没有造成隐藏硬复位。结合三轮jump连续性验收，legacy链冻结为当前安全回退版本，不再追加预测、响应、积分、速度或超时参数补丁。

项目可以继续P0-6只读shadow工作，但这不等于active获准接管。P0-6C必须先实现输出暂停期间影子链连续消费观测，并完成五场景及短/长暂停的DML/NDI shadow验证；在该门禁通过前继续禁止active。
