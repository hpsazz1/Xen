# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

### DML性能阶段（最高优先级）

- [x] 完成最终320/280复验：280 ms左右稳态、高频误差和恢复收益均不足8%，且反转速率差超过5%；冻结现场积分基线320 ms并结束相邻参数搜索，详见 `docs/026冻结320与DML性能诊断20260714.md`。
- [x] 完成首轮6000样本DML性能分析：`session.Run`平均9.411 ms、占旧总耗时97.9%，预处理仅0.170 ms、后处理0.030 ms；主瓶颈确定为模型与DirectML执行，详见 `docs/027DML首轮性能分析与FP16方案20260714.md`。
- [x] 补充 `DmlTensorSetupMs`、`DmlModel`，并将 `DmlTotalMs` 修正为完整墙钟耗时；覆盖层、CSV和分析脚本使用同一线程安全快照。
- [ ] 按 `FP32→FP16→FP16→FP32` 比较 `yolo12n_cs2.onnx` 与 `yolo12n_cs2_fp16.onnx`；每次切换预热30秒，每轮静止、持续右移、高频反转各2000行。
- [ ] FP16需使推理均值或P95至少改善10%、发布FPS至少提高8%、总耗时P95至少改善8%，观测年龄与移动控制指标不得回退超过10%。
- [ ] FP16通过后补静止三点精度冒烟；若未通过，保持FP32并评审DirectML高性能适配器选择与ORT格式模型。

### 后续功能评审
- [ ] 基础静止与移动目标通过上述A/B验收后，单独评审预测补偿模块的行业方案与接入契约；不得在PI参数尚未确定时并行接入预测。
- [ ] 预测模块稳定且有独立开关、诊断字段和回归测试后，再评审轨迹整形模块；不得改变基础控制器稳定性。
