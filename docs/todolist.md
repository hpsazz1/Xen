# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

### DML性能阶段（已结束）

- [x] 完成最终320/280复验：280 ms左右稳态、高频误差和恢复收益均不足8%，且反转速率差超过5%；冻结现场积分基线320 ms并结束相邻参数搜索，详见 `docs/026冻结320与DML性能诊断20260714.md`。
- [x] 完成首轮6000样本DML性能分析：`session.Run`平均9.411 ms、占旧总耗时97.9%，预处理仅0.170 ms、后处理0.030 ms；主瓶颈确定为模型与DirectML执行，详见 `docs/027DML首轮性能分析与FP16方案20260714.md`。
- [x] 补充 `DmlTensorSetupMs`、`DmlModel`，并将 `DmlTotalMs` 修正为完整墙钟耗时；覆盖层、CSV和分析脚本使用同一线程安全快照。
- [x] 完成FP32/FP16各6000样本比较：FP16推理均值改善24.8%、发布FPS提高15.2%，但实际模型输入为640×640而FP32为320×320，且右移稳态P95回退16%、静止水平P95回退40%；不能作为同输入模型替换，保持FP32并按用户决定结束本阶段。
- [x] 增加 `DmlInputWidth/DmlInputHeight` 审计，明确OBS NDI传输的320×320采集尺寸不是FP16模型内部640×640输入的来源。

### 预测模块（当前阶段）
- [x] 以连续真实观测重构独立预测器：自动补偿观测年龄，仅保留启用、额外前瞻和速度平滑三个设置；目标丢失、ID切换、瞄准状态变化、分辨率或跟踪模式变化统一立即重置。
- [x] 增加 `PredictionApplied/PredictionVelocityX/Y/PredictionLeadMs/PredictedX/Y` 诊断和预测生命周期、时间戳、静止、异常跳变、旁路回归测试，控制器修订号升级为5。
- [x] 修复预测设置被编译期条件隐藏且侧栏缺少入口的问题；UI路径固定为“瞄准 → 预测参数”，并增加侧栏、绘制条件和三个控件的自动可见性测试。
- [x] 使用四段2560×1440、240.004 FPS固定视角视频完成DML同模型闭环模拟；稳健覆盖10/15/20 ms观测年龄后，`lead=20 ms/tau=75 ms`使左右稳态P95均值从15.346 px降至12.520 px（改善18.4%），反转与静止约束同时通过，详见 `docs/030标准视频闭环模拟与预测候选20260714.md`。
- [x] 完成20/75/1440首轮实机复测并新增快速横跳：jump两试次P95均值77.04 px、稳态P95均值133.09 px、限速10.1%；预测99.7%生效且预测距离从未触发32 px上限，瓶颈是设备速度预算与跳跃响应，不是预测安全裁剪。
- [x] 将jump视频纳入106 FPS、10/15/20 ms观测年龄联合搜索；筛出 `move_max_speed_cps=2000`、`prediction_lead_ms=10`、`prediction_velocity_tau_ms=75`，模拟jump P95从84.98降至65.46（改善23.0%），普通左右平均改善15.7%，反转和静止不回退，详见 `docs/031快速横跳实测与联合参数优化20260714.md`。
- [x] 完成10/75/2000复测：jump P95由77.04降至66.80（改善13.3%）、限速由10.1%降至5.9%；左右P95分别降至7.27/3.19，反转19.24、静止5.94。设备速度优化有效，但预测偏移P95仅jump 6.12 px、普通移动1~3 px，仍基本位于目标框内。
- [x] 重构框外身位预测：使用自身视角运动补偿还原目标速度；连续三次同向后才启用框外提前，停止或疑似反向首帧立即收回，连续两次反向后才切换预测侧；控制器修订号升级为6。
- [x] 否决目标框测距与固定框外身位：遮挡、只露头部和检测类别会改变框尺寸；逐武器弹速又缺少可靠来源，继续使用会制造伪精度。
- [x] 改为运动学自适应提前：使用补偿后的目标速度与加速度计算 `v×t+½a×t²`，仅保留统一预测强度；停止或疑似反向首帧收回，控制器修订号升级为7。
- [x] 五段视频联合搜索强度后筛出50/15/1.0/2000：jump P95从关闭预测70.35降至16.73，左右均值15.90降至8.45，反转17.98降至8.13，静止X/Y保持1.26/5.58。
- [x] 完成r7首轮实机复测并纠正场景边界：除 `Target_static_Role_reverse` 为目标静止、角色移动外，其余文件角色均静止。static仍有26.7%假预测，Y提前P95 12.31 px；显式加速度和15 ms单帧速度估计会放大检测抖动，详见 `docs/034稳健常速度预测与实机振荡排查20260714.md`。
- [x] r8改为40~120 ms短窗稳健速度回归，默认50 ms；删除加速度控制项，增加净位移/速度/直线性门控和非主轴吸附，停止与反向首帧收回。
- [x] 完成r8复测：left/right、reverse和角色移动场景X误差分别改善约37%、39%和56%，static X假预测降至2.8%；但left/right分别中断215/222次，中位连续预测仅5/4.6帧，单帧反向门控造成开关式晃动，详见 `docs/035预测连续性保持与单帧门控修复20260714.md`。
- [x] r9删除单帧反向控制路径；最后可靠速度可跨两帧窗口退化，连续两帧低速才撤销，反向由稳健回归连续确认；分析器新增预测中断次数和连续运行帧数。
- [x] 完成r9复测：left/right生效率99.8%/99.9%、中断3/1，连续性缺陷解决；static Y假预测反弹到16.6%，jump被切成8段且丢失前目标贴近X=3/315边缘，线性FOV换算同时造成控制少算和运动补偿过量。
- [x] r10统一改用完整源尺寸上的透视像素↔视角换算；最大设备速度范围扩展到4000，下一轮jump使用3200；不允许目标丢失后盲追。
- [x] 完成r10复测：jump首次形成21.65秒完整试次且X P95降至52.68，left/right降至22.26/19.97，角色移动降至17.53；static X/Y预测仍为15.3%/19.9%，剩余问题集中在主动拉回静止目标时的补偿残差。
- [x] r11增加三帧屏幕收敛、自身视角和预测方向联合门控，仅撤销自运动伪迹帧；CSV及分析器新增 `PredictionSelfMotionSuppressed` 比例。
- [x] 完成r11 static九段复测：X/Y P95误差4.61/5.02 px，预测生效率4.7%/4.9%，自运动抑制7.6%；门控命中有效，但trial 8/9命中后沿旧速度立即恢复2~4帧，Y提前P95仍达12 px。
- [x] r12在自运动伪迹命中后继续抑制四个后续观测帧，覆盖约100 FPS下40 ms控制响应尾迹；不改变3200速度、50/50/1.0预测参数、稳健速度或真实移动方向状态。
- [x] 完成r12 static九段复测：X/Y P95误差降至3.31/4.47 px，预测生效率降至2.5%/2.6%，总体指标达标；但trial 9在保持期间重学越过中心后的反向回弹，保持结束后连续9帧反向提前。
- [x] r13在自运动保持尾帧丢弃受控制器过冲影响的速度、方向和回归窗口，仅保留当前观测基点；后续必须使用保持结束后的新观测重新确认方向。
- [x] 完成r13 static九段复测：X/Y P95误差降至2.26/3.57 px，预测生效率0.4%/1.1%，trial 9反向复发消失；trial 4仍以保持尾帧后5.2 px越心回弹重新锁定Y方向并连续提前6帧。
- [x] r14仅在自运动保持后的重新武装阶段将净位移门槛从4 px提高到检测跨度2.5%（320下8 px）；首次预测与普通反向门槛保持不变。
- [x] 完成r14 static九段复测：NDI接收恢复至平均238 FPS；X/Y预测生效率均为0.4%，前八段完全无预测，trial 9仅在首次门控前提前2帧，之后连续抑制并且保持结束后无再次预测，static阶段通过。
- [x] 冻结r14 static参数与门控，不再继续提高保持或重新武装门槛；继续修改的静止收益不足，反而可能扩大真实移动恢复延迟。
- [x] 完成r14 left/right各4轮：X轴P95为26.28/24.23 px，预测生效率63.9%/83.8%，自运动抑制23.6%/10.6%；真实持续横移被静止收敛门控反复误杀，形成预测开关和来回震荡。
- [x] r15增加五帧连续预测后的持续运动豁免；static伪预测仅1~2帧无法获得豁免，真实移动停止、窗口退化或反向后自动清除连续计数。
- [x] 完成r15 left/right各4轮：预测生效率提升至98.0%/98.6%，抑制降至0.2%/0.1%，门控开关已解决；X轴P95仍为22.61/21.68 px，预测误差约每秒穿越中心2次，用户仍可感知来回震荡。
- [x] r16在预测偏移持续有效时保留PI移动积分，避免预测点越心5 px就清空并重建积分；预测停止或反向撤销后立即恢复原有反向解卷绕。
- [ ] r16先复测left/right各4轮，再补static九段；保持3200与预测 `50/50/1.0`、禁止手动鼠标、NDI接收稳定高于220 FPS。
- [ ] r16验收：left/right X轴P95不高于22.26/19.97 px，预测生效率至少95%，输出换向次数和肉眼震荡明显低于r15；static X/Y预测生效率仍不高于0.5%。

### 后续功能评审
- [ ] 预测模块稳定且有独立开关、诊断字段和回归测试后，再评审轨迹整形模块；不得改变基础控制器稳定性。
