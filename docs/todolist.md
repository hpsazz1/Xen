# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

### DML性能阶段（已结束）

- [x] 完成最终320/280复验：280 ms左右稳态、高频误差和恢复收益均不足8%，且反转速率差超过5%；冻结现场积分基线320 ms并结束相邻参数搜索，详见 `docs/026冻结320与DML性能诊断20260714.md`。
- [x] 完成首轮6000样本DML性能分析：`session.Run`平均9.411 ms、占旧总耗时97.9%，预处理仅0.170 ms、后处理0.030 ms；主瓶颈确定为模型与DirectML执行，详见 `docs/027DML首轮性能分析与FP16方案20260714.md`。
- [x] 补充 `DmlTensorSetupMs`、`DmlModel`，并将 `DmlTotalMs` 修正为完整墙钟耗时；覆盖层、CSV和分析脚本使用同一线程安全快照。
- [x] 完成FP32/FP16各6000样本比较：FP16推理均值改善24.8%、发布FPS提高15.2%，但实际模型输入为640×640而FP32为320×320，且右移稳态P95回退16%、静止水平P95回退40%；不能作为同输入模型替换，保持FP32并按用户决定结束本阶段。
- [x] 增加 `DmlInputWidth/DmlInputHeight` 审计，明确OBS NDI传输的320×320采集尺寸不是FP16模型内部640×640输入的来源。

### 预测模块（当前阶段）
- [x] 以连续真实观测重构独立预测器：自动补偿观测年龄，仅保留启用、额外前瞻和速度平滑三个设置；目标丢失、ID切换、瞄准状态变化、分辨率或跟踪模式变化统一立即重置。
- [x] 增加 `PredictionApplied/PredictionVelocityX/Y/PredictionLeadMs/PredictedX/Y` 诊断和预测生命周期、时间戳、静止、异常跳变、旁路回归测试，控制器修订号升级为5。
- [x] 修复预测设置被编译期条件隐藏且侧栏缺少入口的问题；UI路径固定为“瞄准 → 预测参数”，并增加侧栏、绘制条件和三个控件的自动可见性测试。
- [x] 使用四段2560×1440、240.004 FPS固定视角视频完成DML同模型闭环模拟；稳健覆盖10/15/20 ms观测年龄后，`lead=20 ms/tau=75 ms`使左右稳态P95均值从15.346 px降至12.520 px（改善18.4%），反转与静止约束同时通过，详见 `docs/030标准视频闭环模拟与预测候选20260714.md`。
- [x] 完成20/75/1440首轮实机复测并新增快速横跳：jump两试次P95均值77.04 px、稳态P95均值133.09 px、限速10.1%；预测99.7%生效且预测距离从未触发32 px上限，瓶颈是设备速度预算与跳跃响应，不是预测安全裁剪。
- [x] 将jump视频纳入106 FPS、10/15/20 ms观测年龄联合搜索；筛出 `move_max_speed_cps=2000`、`prediction_lead_ms=10`、`prediction_velocity_tau_ms=75`，模拟jump P95从84.98降至65.46（改善23.0%），普通左右平均改善15.7%，反转和静止不回退，详见 `docs/031快速横跳实测与联合参数优化20260714.md`。
- [x] 完成10/75/2000复测：jump P95由77.04降至66.80（改善13.3%）、限速由10.1%降至5.9%；左右P95分别降至7.27/3.19，反转19.24、静止5.94。设备速度优化有效，但预测偏移P95仅jump 6.12 px、普通移动1~3 px，仍基本位于目标框内。
- [x] 重构框外身位预测：使用自身视角运动补偿还原目标速度；连续三次同向后才启用框外提前，停止或疑似反向首帧立即收回，连续两次反向后才切换预测侧；控制器修订号升级为6。
- [x] 否决目标框测距与固定框外身位：遮挡、只露头部和检测类别会改变框尺寸；逐武器弹速又缺少可靠来源，继续使用会制造伪精度。
- [x] 改为运动学自适应提前：使用补偿后的目标速度与加速度计算 `v×t+½a×t²`，仅保留统一预测强度；停止或疑似反向首帧收回，控制器修订号升级为7。
- [x] 五段视频联合搜索强度后筛出50/15/1.0/2000：jump P95从关闭预测70.35降至16.73，左右均值15.90降至8.45，反转17.98降至8.13，静止X/Y保持1.26/5.58。
- [x] 完成r7首轮实机复测并纠正场景边界：除 `Target_static_Role_reverse` 为目标静止、角色移动外，其余文件角色均静止。static仍有26.7%假预测，Y提前P95 12.31 px；显式加速度和15 ms单帧速度估计会放大检测抖动，详见 `docs/034稳健常速度预测与实机振荡排查20260714.md`。
- [x] r8改为40~120 ms短窗稳健速度回归，默认50 ms；删除加速度控制项，增加净位移/速度/直线性门控和非主轴吸附，停止与反向首帧收回。
- [x] 完成r8复测：left/right、reverse和角色移动场景X误差分别改善约37%、39%和56%，static X假预测降至2.8%；但left/right分别中断215/222次，中位连续预测仅5/4.6帧，单帧反向门控造成开关式晃动，详见 `docs/035预测连续性保持与单帧门控修复20260714.md`。
- [x] r9删除单帧反向控制路径；最后可靠速度可跨两帧窗口退化，连续两帧低速才撤销，反向由稳健回归连续确认；分析器新增预测中断次数和连续运行帧数。
- [x] 完成r9复测：left/right生效率99.8%/99.9%、中断3/1，连续性缺陷解决；static Y假预测反弹到16.6%，jump被切成8段且丢失前目标贴近X=3/315边缘，线性FOV换算同时造成控制少算和运动补偿过量。
- [x] r10统一改用完整源尺寸上的透视像素↔视角换算；最大设备速度范围扩展到4000，下一轮jump使用3200；不允许目标丢失后盲追。
- [x] 完成r10复测：jump首次形成21.65秒完整试次且X P95降至52.68，left/right降至22.26/19.97，角色移动降至17.53；static X/Y预测仍为15.3%/19.9%，剩余问题集中在主动拉回静止目标时的补偿残差。
- [x] r11增加三帧屏幕收敛、自身视角和预测方向联合门控，仅撤销自运动伪迹帧；CSV及分析器新增 `PredictionSelfMotionSuppressed` 比例。
- [x] 完成r11 static九段复测：X/Y P95误差4.61/5.02 px，预测生效率4.7%/4.9%，自运动抑制7.6%；门控命中有效，但trial 8/9命中后沿旧速度立即恢复2~4帧，Y提前P95仍达12 px。
- [x] r12在自运动伪迹命中后继续抑制四个后续观测帧，覆盖约100 FPS下40 ms控制响应尾迹；不改变3200速度、50/50/1.0预测参数、稳健速度或真实移动方向状态。
- [x] 完成r12 static九段复测：X/Y P95误差降至3.31/4.47 px，预测生效率降至2.5%/2.6%，总体指标达标；但trial 9在保持期间重学越过中心后的反向回弹，保持结束后连续9帧反向提前。
- [x] r13在自运动保持尾帧丢弃受控制器过冲影响的速度、方向和回归窗口，仅保留当前观测基点；后续必须使用保持结束后的新观测重新确认方向。
- [x] 完成r13 static九段复测：X/Y P95误差降至2.26/3.57 px，预测生效率0.4%/1.1%，trial 9反向复发消失；trial 4仍以保持尾帧后5.2 px越心回弹重新锁定Y方向并连续提前6帧。
- [x] r14仅在自运动保持后的重新武装阶段将净位移门槛从4 px提高到检测跨度2.5%（320下8 px）；首次预测与普通反向门槛保持不变。
- [x] 完成r14 static九段复测：NDI接收恢复至平均238 FPS；X/Y预测生效率均为0.4%，前八段完全无预测，trial 9仅在首次门控前提前2帧，之后连续抑制并且保持结束后无再次预测，static阶段通过。
- [x] 冻结r14 static参数与门控，不再继续提高保持或重新武装门槛；继续修改的静止收益不足，反而可能扩大真实移动恢复延迟。
- [x] 完成r14 left/right各4轮：X轴P95为26.28/24.23 px，预测生效率63.9%/83.8%，自运动抑制23.6%/10.6%；真实持续横移被静止收敛门控反复误杀，形成预测开关和来回震荡。
- [x] r15增加五帧连续预测后的持续运动豁免；static伪预测仅1~2帧无法获得豁免，真实移动停止、窗口退化或反向后自动清除连续计数。
- [x] 完成r15 left/right各4轮：预测生效率提升至98.0%/98.6%，抑制降至0.2%/0.1%，门控开关已解决；X轴P95仍为22.61/21.68 px，预测误差约每秒穿越中心2次，用户仍可感知来回震荡。
- [x] r16在预测偏移持续有效时保留PI移动积分，避免预测点越心5 px就清空并重建积分；预测停止或反向撤销后立即恢复原有反向解卷绕。
- [x] 完成r16 left/right复测：X轴P95显著降至13.31/9.25 px，预测生效率97.7%/98.6%，最终输出换向降到多数轮次0次，肉眼震荡明显减小。
- [x] 排除检测框宽度直接驱动水平抖动：宽度帧差与枢轴X帧差相关系数仅约-0.03；剩余约1.5 px支点漂移主要来自检测中心单帧跳变被瞬时高速响应放大。
- [x] r17使用约30 ms有符号速度趋势控制基础滤波响应；持续同向运动快速放宽，交替检测跳点不会因单帧高速度立即穿透，并新增 `FilterTrendSpeed` CSV诊断。
- [x] 完成r17 left/right复测：X轴P95进一步降至8.29/8.88 px，预测生效率99.0%/98.8%，设备输出每轮仅0~1次换向；趋势速度P95稳定302~319 px/s，而瞬时速度P95最高1338 px/s，检测尖峰已被隔离。
- [x] 冻结r17移动滤波、预测与积分参数，不再继续压低left/right误差；当前收益明确且无新增跟随滞后，继续调参风险高于轻微残余收益。
- [x] 完成r17 static九段复测：X/Y预测生效率回退至1.7%/2.1%，Y轴P95升至7.93 px；误预测仍仅1~2帧，但趋势滤波在预测建立前的主动收敛阶段造成位置拖尾。
- [x] r18仅在上一帧已有非零预测偏移时启用趋势滤波；static和移动初始收敛使用原瞬时响应，持续移动保留r17去跳点，并新增 `FilterTrendActive` CSV诊断。
- [x] 完成r18 static九段复测：X/Y预测生效率仍为2.1%，所有残余预测段仅1~3帧，趋势滤波只在伪预测后一帧开启并非初始根因；方向初步锁定后过早释放提前量。
- [x] r19要求方向锁定后再连续收到三次可靠方向才释放提前量；确认前基础控制照常跟随但预测偏移为零，持续运动计数也从正式释放后开始。
- [x] 完成r19 static九段复测：X/Y预测生效率降至0.6%，仅trial 4输出2帧、trial 8输出1帧，全部位于第三次可靠方向刚释放后并立即被门控撤销。
- [x] r20将正式释放阈值从三次提高到四次可靠方向确认，约多10 ms；预计消除确认边界的3个残余帧，不修改其余预测和控制逻辑。
- [x] 完成r20 static九段复测：481个有效样本中X/Y框外预测均为0，误预测生效率从r19的0.6%降至0%；X/Y平均P95为4.44/5.11 px，NDI接收平均238.3 FPS，冻结static方向确认参数。
- [x] 修复移动分析器在某一轴完全没有请求位移时因空数组中止的问题；以0明确表示该试次无法估算counts/px和闭环滞后，不把正常零位移误判为数据损坏。
- [x] 完成r20 left/right复测：实际切分为left 5轮、right 4轮，X轴平均P95为7.62/8.49 px，预测生效率96.4%/97.8%，均通过8.29/8.88 px与94%的验收线。
- [x] 排除left/right各一轮高P95为持续震荡：离群轮稳态P95均为5.04 px，两次设备输出换向只发生在开始后259~309 ms且预测偏移仍为0；正式预测方向翻转总数为0。
- [x] 冻结r20预测基础参数 `50/50/1.0/3200`：static误预测为0%，持续左右移动优于r17基线，不再为初始捕获瞬态增加预测复杂度或延迟。

### 后续功能评审
- [x] 完成r20 reverse与horizontal_jump基线：reverse X轴P95 17.81 px、预测中位连续2帧、36次换边和83次中断；jump X/Y P95 118.61/36.62 px、限速10.9%，预测提前P95达到112 px。
- [x] r21增加七个有效帧的提前量渐进释放，并将异常高速提前上限从检测跨度35%降至20%；不改变方向确认、目标丢失、基础PI、速度参数或普通持续移动的全额提前。
- [x] 完成r21复测：jump X轴P95由118.61降至73.47 px、限速由10.9%降至4.9%，但64 px提前仍偏大；reverse X轴P95仍为17.90 px，而去除预测偏移后的目标框中心P95仅10.96 px。
- [x] r22自动识别1.5秒内三次可靠换向并撤销高频摆动提前量；jump最短三次换向跨度1.847秒，不命中该门控。高速提前上限由64 px收紧到24 px。
- [x] 完成r22复测并定位门控实现偏差：reverse抑制命中0%、仍有21%预测；jump反而误命中14.2%。原因是只记录锁定状态内直接翻转，遗漏reverse常见的解锁后反向重建。
- [x] r23在统一方向锁定入口跨解锁比较最后可靠方向；垂直误差达到16 px时仅将Y轴比例响应由80 ms临时缩短到40 ms，并新增有效Y响应与`VerticalCatchUp`诊断。
- [x] 完成r23复测：reverse抑制95.9%、预测仅1.5%、X轴P95降至11.81 px且预测换边为0；jump Y轴P95由27.76降至22.96 px、闭环滞后降至55.8 ms，但仍误触往返门控15.5%，X轴预测继续放大误差。
- [x] r24仅将速度不超过检测跨度/秒的反转计入高频往返；连续三个观测同时超过检测跨度1.25倍/秒和5倍跨度/秒方时撤销不可信外推，稳定高速直线运动与单帧异常仍保留预测。
- [x] 完成r24复测：reverse水平准星99.5%在框内、往返抑制94.6%、预测2.4%，但仍追逐框内pivot造成视觉摆动；jump目标框中心X/Y P95仍为72.60/15.98 px，水平基础响应成为主瓶颈。
- [x] r25在高频往返抑制期间改为检测框内保持：框内不移动，越界只追到最近内边缘；16 px大误差40 ms追赶从Y轴扩展为逐轴独立X/Y响应。
- [x] 完成r25复测：jump X/Y P95降至38.76/16.32 px，但X轴P99仍77.91 px；reverse在1.63秒进入框内保持后平均水平输出由3.70降至0.76 counts，76.8%帧完全不动，后段过度被动。
- [x] r26将reverse水平保持区收紧为检测框中央50%，Y轴仍使用完整框边缘；逐轴追赶增加32 px以上20 ms第二档，16~32 px继续40 ms。
- [x] 完成r26复测：jump X/Y P95回退至45.19/19.16 px、X P99升至91.51 px，20 ms档68.3%帧限速；reverse抑制后目标中心误差均值由6.62升至11.65 px且69.9%帧无水平输出，仍有后段变慢。
- [x] r27将reverse水平走廊收紧为中央20%；jump取消20 ms硬档，16~32 px由40 ms连续过渡至30 ms，32 px以上保持30 ms，消除阈值增益跳变和持续饱和。
- [x] 完成r27复测：reverse抑制后零水平输出降至40.2%、目标中心X P95降至18.80 px，中央20%走廊通过并冻结；jump X/Y P95为53.62/18.34 px，限速7.5%，预测46次中断且连续预测中位仅3帧。
- [x] r28对已持续预测、当前样本与稳健方向同向的高速加速首帧立即撤销提前量；反向跳点、停止和刚重建方向仍保留三帧确认，基础跟踪与r27连续追赶不变。
- [x] 完成r28单轮复测：jump X/Y P95降至37.54/15.75 px、X P99降至51.30 px、限速降至3.7%；但本轮目标速度P95和检测残差P95也分别由2500/27.88降至1908.5/23.52，且仅形成1个试次，先冻结代码避免过拟合。
- [x] 完成r28第二次独立jump采集：X/Y P95为37.54/17.05 px、X P99为64.83 px、限速4.1%，目标X速度P95升至2103.2 px/s后仍稳定通过；两次独立上传结果一致，冻结r28预测模块。
- [x] 确认流水线2000帧环形缓冲无法依靠松开瞄准键拆分试次：最新文件恰好2000行、FrameID连续699~2698、最大帧间隔仅19 ms。后续每轮测试必须清空缓冲并单独导出CSV。
- [x] 完成r28单向回归：left/right预测分别中断21/23次，连续预测中位仅2/3帧；高速抑制段速度中位仅473.8/461.3 px/s，旧400 px/s门槛把稳定单向回归加速度误判为jump，用户观察到明显错误预测。
- [x] r29将高速瞬态速度门槛由检测跨度1.25倍/秒提高到2倍/秒，即320检测跨度下由400提高到640 px/s；加速度门槛、可信同向首帧锁存和三帧保护保持不变。
- [x] 完成r29复测：left预测生效率升至80.9%、中断由21降至5、X P95降至16.75 px；jump三试次X/Y最差P95为30.74/18.91 px且限速最高1.2%。right高速误触降至2.2%，但43~49 ms短反向及恢复被连续计为换向，误触21.1%高频抑制，X P95升至28.48 px。
- [x] r30要求新可靠方向至少保持80 ms后才允许再次计入反转，过滤单向闭环的短回摆及其恢复；1.5秒三次换向窗口、真实reverse抑制和中央20%走廊不变。
- [ ] r30复测`horizontal_right_12.csv` 1轮，要求高频抑制接近0、中断明显低于16、X P95低于28.48 px；随后复测reverse 1轮确认抑制仍能建立，left/jump无需重复调参。
