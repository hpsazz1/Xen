# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

### 下一轮测试优化（最高优先级）

- [x] 完成最新 `XenMovingPI320`/`XenMovingPI240` 对照分析：默认反转阈值由12 px修正为10 px后，240 ms识别23次真实反转；240 ms持续跟随和高频误差改善，但恢复P95从76 ms回退到90 ms，当前保持320 ms，详见 `docs/024反转阈值与最新积分复测20260714.md`。
- [ ] 建立可重复移动脚本：固定检测分辨率320、完整源2560×1440、目标移动速度约470 counts/s、单向试次4秒、高频反转10秒，并固定左右起始误差绝对值为80±5 px。价值是消除本轮0～151 px起点差异造成的整段P95偏差；脚本无法重复时暂停参数比较，不继续调参。
- [ ] 执行320/280 ms积分时间ABBA单变量对照：保持 `move_response_ms=80`、`move_max_speed_cps=1440` 和其他配置不变，按 `320→280→280→320` 顺序采集。两档数据分别放入 `XenMovingPI320/DML/ndi` 与 `XenMovingPI280/DML/ndi`，每档包含 `horizontal_left.csv`、`horizontal_right.csv`、`horizontal_reverse.csv`。
- [ ] 每份CSV采集前核对UI标题和首行身份：`BuildBackend=DML`、UI短提交与 `BuildRevision` 一致、`ControllerRevision=4`、无 `-dirty`；320/280两档及全部ABBA轮次必须使用同一 `BuildRevision`。纯非代码提交后允许该版本早于仓库当前 `HEAD`，无需重建；只有身份互相不一致、带 `-dirty`，或版本早于最近一次程序相关变更时才判定整组作废。价值是确保A/B只比较配置，不混入程序版本差异。
- [ ] 单向场景每档左右各完成3轮，每轮持续4秒、轮间松开瞄准至少300 ms；主要比较 `MeanSteadyP95AbsAxisErrorPx`，辅助比较整段P95、闭环滞后、`MovingInsideSettlePct`、限速和队列。280 ms只有在左右稳态P95平均至少改善8%，且任一方向不回退超过10%时才进入下一验收阶段。
- [ ] 高频反转每档连续10秒，反转速率固定为2.5±0.15次/秒，两档差异不得超过5%；分析使用默认±10 px并连续3帧确认。要求 `RecoveredReversals=ReversalCount`、队列最大深度不超过1、限速为0；280 ms高频误差P95至少改善8%，恢复P95不得高于320基线10%。
- [ ] 对通过移动门槛的积分档位执行DML+NDI静止冒烟：上中、左下、右下各3轮；要求稳定态退出为0、稳定后最大误差不超过8 px、无持续反向积分。280 ms若静止任一项失败，则保持320 ms，不再通过调整稳定半径掩盖回归。
- [ ] 使用 `tools/analyze_moving_target.ps1` 分别导出320/280两档机器可读汇总，并记录单向起始误差、反转速率、原始指标、相对变化、静止结果和最终选择。只有两次280与两次320结果方向一致，才修改现场推荐值。

### 后续性能与功能评审

- [ ] 统一采集统计经NDI、Desktop Duplication和WinRT实机复测后，单独基准分析DML预处理、推理和后处理；触发条件是 `FPS` 稳定高于 `InferenceFPS` 30%以上。当前NDI场景源到达约220～234 FPS、DML推理约88～92 FPS，已满足性能分析触发条件，但优先级低于积分A/B闭环。
- [ ] 基础静止与移动目标通过上述A/B验收后，单独评审预测补偿模块的行业方案与接入契约；不得在PI参数尚未确定时并行接入预测。
- [ ] 预测模块稳定且有独立开关、诊断字段和回归测试后，再评审轨迹整形模块；不得改变基础控制器稳定性。
