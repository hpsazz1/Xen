# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

- [ ] 统一采集统计经 NDI、Desktop Duplication 和 WinRT 实机复测后，单独基准分析 DML 预处理、推理和后处理；触发条件是 `FPS` 稳定高于 `InferenceFPS` 30% 以上。
- [ ] 基础静止与移动目标测试通过后，单独评审预测补偿模块的行业方案与接入契约。
- [ ] 预测模块稳定后，再评审轨迹整形模块；不得改变基础控制器稳定性。
- [ ] 1440 counts/s 四链路静止九宫格复测通过后，增加固定角速度横移目标，统计跟随误差P95和方向反转恢复时间；用于验证更高远距速度对动态目标的收益与边界。
- [ ] CUDA+NDI 与 CUDA+UDP 各重复三次相同九宫格，以位置中位数和P95验证本轮约24 ms传输差异是否可重复；若差异不稳定，不为两种传输拆分控制参数。
