# 非阻塞待办

本文件只记录不阻塞当前核心功能上线的细节。核心缺陷不得转移到此处规避处理。

## 待办格式

- `[ ]` 事项：说明价值、触发条件和建议处理阶段。

## 当前待办

### DML性能阶段（最高优先级）

- [x] 完成最终320/280复验：280 ms左右稳态、高频误差和恢复收益均不足8%，且反转速率差超过5%；冻结现场积分基线320 ms并结束相邻参数搜索，详见 `docs/026冻结320与DML性能诊断20260714.md`。
- [x] 增加线程安全DML阶段耗时快照、六项流水线CSV字段和正式性能分析脚本；旧CSV缺少阶段字段时不得用于性能决策。
- [ ] 使用新DML构建保持320×320、2560×1440、NDI和80/320/1440，采集静止中心、持续移动、高频反转各30秒，每份至少1000个有效耗时样本。
- [ ] 运行 `tools/analyze_dml_performance.ps1` 导出汇总；若推理占比达到70%，优先优化模型/ORT/DirectML，只有预处理或后处理占比达到15%时才进入对应CPU路径。
- [ ] 目标阶段均值或P95至少改善10%、检测发布FPS至少提高8%，同时观测年龄和移动控制指标不得回退超过10%。
- [ ] DML+NDI取得收益后，再单变量切换Desktop Duplication或WinRT复测；不得同时修改模型、检测分辨率和采集方式。

### 后续功能评审
- [ ] 基础静止与移动目标通过上述A/B验收后，单独评审预测补偿模块的行业方案与接入契约；不得在PI参数尚未确定时并行接入预测。
- [ ] 预测模块稳定且有独立开关、诊断字段和回归测试后，再评审轨迹整形模块；不得改变基础控制器稳定性。
