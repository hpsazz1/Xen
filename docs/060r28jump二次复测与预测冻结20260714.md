# r28 jump 二次复测与预测冻结

## 背景

用户按计划再次上传`horizontal_jump.csv`。数据仍来自DML构建`1788db06c01d`、控制器r28。本轮文件包含2000行、约21.6秒数据。

## 数据完整性

- 文件恰好达到流水线UI缓冲上限2000帧。
- FrameID从699连续到2698，没有重置或缺口。
- 相邻记录最大时间间隔仅19 ms，没有超过分析器150 ms拆分阈值的空档。
- 因此分析器只能得到1个连续试次。松开瞄准键不会停止流水线记录，不能用于在同一导出文件中标记轮次。

后续多轮测试不得继续依赖单文件内的松键间隔。每轮应先清空流水线缓冲，完成一轮后立即单独导出CSV，再开始下一轮。

## r28两次独立结果

第一次r28采集：

- X/Y P95为37.54/15.75 px。
- X P99为51.30 px，限速率3.7%。
- 目标X速度P95为1908.5 px/s，检测残差P95为23.52 px。

第二次r28采集：

- X/Y P95为37.54/17.05 px。
- X P99为64.83 px，限速率4.1%。
- 目标X速度P95升至2103.2 px/s，检测残差P95为23.02 px。

两次独立上传的X P95完全一致；第二次目标速度更高时，Y P95、X P99和限速仍全部低于r27验收上限18.34 px、109.87 px和7.5%。r28高速瞬态门控在不同动作强度下表现稳定。

## 预测诊断

- 第二次预测生效率11.0%，高速瞬态抑制29.0%，自运动抑制41.8%。
- 预测中断73次、连续预测中位2帧，但整体误差和尾部仍稳定通过。
- 中断次数随采集时长、目标加速频率和门控覆盖变化，当前没有证据表明它应作为独立优化目标。
- 继续为了降低中断次数放宽预测，会重新释放已被r28证明不可靠的短高速提前量。

## 结论

冻结干净程序提交`1788db06c01d / r28`，预测模块不再修改。r28相对r27的改善已在两次独立上传中复现，继续调参的预期收益低于回退风险。

## 下一阶段

- 清空流水线缓冲后测试`horizontal_left_1.csv`，持续至少5秒并立即导出。
- 同样独立导出`horizontal_left_2.csv`、`horizontal_right_1.csv`、`horizontal_right_2.csv`。
- 最后独立导出`horizontal_reverse_1.csv`，只确认中央20%走廊冻结指标未回退。
- 单向场景重点检查X/Y P95、预测连续性和限速率；reverse检查框内稳定与后段输出连续性。

## 变更说明

本轮只有数据分析、冻结结论和测试流程文档更新，没有源码、脚本、配置或运行行为变更。无需重新构建或推送，测试程序身份继续使用`1788db06c01d / r28`。
