# r59暂停连续影子最终验收

## 补充批次

2026-07-16 23:40～23:41上传的`ndi/new`包含`horizontal_left.csv`和`horizontal_right.csv`各2000帧，构建身份统一为`DML / 8db8c8b0f5ea / r59`。`8db8c8b`相对首轮`a2817c1`只修改离线分析脚本和文档，C++运行算法、配置和CSV生产契约没有变化，因此两个身份必须分批通过安全验收，但可以汇总同一r59算法的场景覆盖证据。

新批次4000帧全部为有效shadow估计，模式、身份、时间顺序、Kalman数值、后续阶段组件和构建一致性违例均为0。2287个暂停观测的设备命令、发送、应用、队列和观测序列违例均为0，暂停时长时间戳违例为0。

## 暂停覆盖与恢复质量

新批次恢复事件为：

- left：短暂停1次、350～2000 ms长暂停10次；
- right：短暂停6次、350～2000 ms长暂停7次；
- 24个恢复首帧的shadow估计全部有效，恢复NIS X最大1.96；
- 两处暂停末帧与恢复帧速率符号变化均有原始pivot支持：一处从接近零速开始左移，另一处在1.52秒暂停中真实发生往返、恢复前再次转左。两者innovation/NIS正常、代次和序列连续，不属于复位或估计器误翻向。

与首轮独立安全批次汇总后：

| 方向 | 短暂停 | 350～2000 ms长暂停 | 门槛 |
|---|---:|---:|---:|
| left | 11 | 12 | 各至少6，PASS |
| right | 13 | 12 | 各至少6，PASS |

两批合计12553帧、5563个暂停观测、59个恢复边界，所有恢复首帧估计有效，暂停命令和序列违例均为0。P0-6C DML实机影子门禁正式完成。

## 阶段结论

P0-6C只证明输出暂停期间的连续观测与零设备越界，可以关闭该待办，不需要继续修改r59或补采暂停数据。当前仍不能直接启用active：总计划要求P0-5跨域门槛全部通过，而现有自适应Q基线仍为688/810，static、jump和reverse存在未关闭变体。下一主线应回到P0-5，基于真实DML shadow的NIS/误差分层与跨域失败变体对齐，逐一处理估计模型或验收口径；在P0-5解除`HOLD_SHADOW`前，不执行P反馈active，也不进入CUDA shadow。
