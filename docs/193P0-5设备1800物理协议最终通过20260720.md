# P0-5设备1800物理协议最终通过

## 验收范围与证据

本阶段只验收指定设备、NDI源、ROI和构建身份下的隔离物理速率能力，不修改正式控制器速度、配置或active。权威上传包为`C:\Users\16143\Desktop\Xen\DML\ndi\7\result_8609041_01.zip`，SHA-256为`A682C403EF67451C6F17B34D467A231C23B8E27B30F8D3344DD37BCF6FCDC9EE`。

协议身份为`DML|860904143508|20260720T102158Z|r65|f18d5961a1784d5db7d4e571f813123e`。计划绑定`KMBOX_NET:7679E04E`、`HPSAZZ (main)`、ROI `120,100,80,80`、240 Hz、1440先行、人工计划ID确认和1800后行。执行器原始decision为`ProtocolCompleted=1`、`Recommendation=MANUAL_REVIEW_ONLY`、`ConfigurationAutoWrite=0`、`ActiveAutoEnable=0`且`Issues`为空。

上传包内关键文件SHA-256如下：

- `recovery_speed_device_summary.csv`：`22DF2A24391CE3C320C5EF853E4C18E04596D851BCB6EBE79A67B1D5D4AB5759`
- `recovery_speed_device_commands.csv`：`69B430F0ACF0D18622DD84EE1D069FAA70E19B4B51AC49CE5F52ED9A4BFC4252`
- `recovery_speed_device_frames.csv`：`BBD5B41162655B291BD80E90E38C75263D807392D87DD704932AC166D9FA3E6C`
- `recovery_speed_device_decision.txt`：`4B3123B5E7596D781962656C508D6B7C9FFA3E195B02A7492B815C2AAF03A9F7`
- `recovery_speed_device_analysis.txt`：`12A727F104406C7BDD90CEBF5C1CAE85E8DC9AAE54504B176F66AA05711AB8AA`

## 四trial结果

1440正方向稳定终点/峰值为25/25 px，响应延迟15.8638 ms，过冲0 px，比例0.520833 px/count，残差0 px，串轴泄漏0%。1440负方向为25/25 px、15.2124 ms、0 px、0.520833 px/count、0 px和4%。两方向比例完全一致，均通过。

1800正方向稳定终点/峰值为31/31 px，响应延迟13.7081 ms，过冲0 px，比例0.516667 px/count，残差0 px，串轴泄漏3.225806%。1800负方向为31/31 px、21.2126 ms、0 px、0.516667 px/count、0 px和0%。两方向比例完全一致，均通过。

四trial共64条命令全部成功，最大绝对调度抖动0.016 ms；2908帧全部有效，最低跟踪质量0.810709；比例范围0.516667～0.520833 px/count，最坏过冲0 px、最终残差0 px、串轴泄漏4%。全部指标分别低于4.2 ms、至少0.75、位于0.25～0.75、低于12 px、低于3 px和低于10%的固定门槛。1800相对1440的双方向过冲增量均为0，跨速率门禁通过。

## 独立复核

仓库正式`tools/analyze_recovery_speed_device_protocol.ps1`对解压数据使用`-RequirePass`独立执行，得到4条summary、64条命令、2908帧，统一身份仅一项，所有帧有效且跟踪质量合格，四trial形状、命令序列、counts、调度、比例、峰值、过冲、残差、串轴、方向对称和跨速率比较全部通过。独立结果为`ProtocolPassed=1`、`Recommendation=MANUAL_REVIEW_ONLY`且`Issues`为空。

程序提交`8609041435081b89c8f87b9b668036be4f27c2f3`已推送并与`origin/master`一致；最终DML/CUDA Release均clean-first构建，双后端CTest各17/17通过。DML构建身份为`DML|860904143508|20260720T102158Z|r65`，DML执行器SHA-256为`D5F2D401BF3A9DD797D8D57CDC476FD7B72AB092A74060B350505ACA876D5CCA`。

## 结论与后续边界

指定KMBOX设备在本隔离协议、当前NDI链路和ROI下完成1440与1800 counts/s正负方向物理验证，设备1800双方向能力门禁关闭，最终结论为`MANUAL_REVIEW_ONLY`。此前190～192号发现的固定停止锚点、确认期间NDI积压和已提交响应误计为过冲问题均已通过真实重测闭环。

该结论只消除“设备是否能安全执行1800隔离脉冲”的未知项，不证明正式恢复控制在全部static、jump、reverse和检测重捕获域中具有净收益。正式控制器仍保持1440 counts/s，配置、机器Profile、shadow和active均不自动修改。后续若要把1800用于正式恢复路径，必须另立跨域shadow-only质量协议，复用既有共同队列、原通过零损失和双方向质量门禁；在该协议通过前不得把本次硬件能力结论直接转换为配置变更。
