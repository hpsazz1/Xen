# r63三场景实机影子门禁完成

## reverse数据与身份

用户上传 `C:\Users\16143\Desktop\Xen\DML\ndi\reverse\reverse_r63_positive_fast.csv`，作为当前能够采集到的最快 reverse 正样本。文件共 2576 帧，身份为 `DML / 540a31bcaced / r63`，与已通过 static 和 jump 的正式程序一致。

逐帧配置为 shadow、20/20 ms、`maneuver_gated_ca + 8000/12/120`、不确定度增益 1.25、尾部 20 ms，Profile 标定关闭。构建身份、模式、时序、估计有限性、暂停零命令、暂停连续观测、门控选择、响应参数和逐轴不确定度乘积均为 0 违例，文件级结果为 PASS。

## reverse覆盖

文件包含 1837 个运行态观测和 739 个暂停态观测，运行态机动启用 879 帧、暂停态启用 674 帧。采集形成 6 个完整运行段，帧数分别为 199、265、410、362、349、252；对应运行态启用帧为 88、121、230、160、160、120，覆盖 6/6。门控证据峰值为 41.45、33.02、38.13、28.45、32.79、35.57 度/秒，均明显越过 12 度/秒物理门槛。

运行态 settled 启用为 0 不构成失败：该批 reverse 持续处于真实运动状态，正样本要求是每个完整段能够启用机动模型，并不要求运动期间进入静止收敛态。不要求用户继续提高 reverse 速度。

## 三场景总门禁

将以下三份同身份文件隔离后运行正式分析器：

- `static_r63.csv`；
- `jump_r63_positive.csv`；
- `reverse_r63_positive_fast.csv`。

总计 7749 帧和 7749 个有效估计，其中暂停态 2783 帧。static 的 9/9 运行段保持机动启用 0；jump 6/6 完整段覆盖；reverse 6/6 完整段覆盖。jump 与 reverse 合计 1680 个运行态启用帧，全部场景的模式、身份、时序、诊断、暂停零命令、暂停连续观测、选择计数、1.25 增益、20 ms 尾部及逐轴不确定度契约均为 0 违例，文件级和 overall 均为 PASS。

因此 r63 完成“static 不误触、jump/reverse 真机动不漏检”的最小实机闭环。20 ms 是当前通过正负样本的最小尾部，不继续调参，也无需追加 left/right 或更快 reverse。

## 后续边界

本结论只完成 P0-6F 机动模型选择的实机影子门禁，不代表整条新控制链可以切换 active。项目仍有 P0-5 跨域回放晋级项未完成，且运行时 active 继续强制降级为 shadow、设备正式输出继续由 legacy 产生。下一步应回到剩余的跨域回放失败域和 active 晋级条件，不再重复采集本轮三场景。
