# r63 jump正样本实机通过

## 背景与身份

用户上传 `C:\Users\16143\Desktop\Xen\DML\ndi\jump\jump_r63_positive.csv`，验证 r63 的 20 ms 不确定度尾部是否过度抑制真实 jump 机动。文件共 3040 帧，身份为 `DML / 540a31bcaced / r63`，与通过 static 门禁的正式程序一致。

逐帧配置继续保持 shadow、20/20 ms、`maneuver_gated_ca + 8000/12/120`、增益 1.25、尾部 20 ms和 Profile 标定关闭。构建身份、模式、时序、估计有限性、暂停零命令、暂停连续观测、门控选择、响应参数和逐轴不确定度乘积均为 0 违例，正式分析器文件级结果为 PASS。

## 覆盖结果

文件有 2310 个运行态观测和 730 个暂停态观测，运行态机动启用 801 帧，暂停态启用 527 帧。采集切分出 7 个运行段，其中第 5 段仅 9 帧，判定为一次短误触；其余 6 个完整段分别为 380、521、401、350、316、333 帧。

6 个完整段的运行态机动启用帧分别为 156、89、121、155、121、150，覆盖 6/6；9 帧误触段也有 9 帧启用。各段门控证据峰值分别为 50.00、67.66、72.17、88.30、39.74、70.01、79.73 度/秒，均形成明确的物理机动正样本。运行态 settled 启用为 0 不构成失败：jump 正样本门禁要求真实运动期间覆盖机动模型，而不是要求目标进入静止收敛态。

因此 r63 jump 正样本正式通过，20 ms 尾部没有抹除真实 jump。隔离单文件运行时 overall 汇总仍会提示缺少 static/reverse 场景，这是跨文件总门禁，不影响本文件 PASS。

## 下一步

最后复测用户当前能够采集的最快 reverse。保持相同程序和配置，开始前重置流水线一次；采集 4 个独立完整运行段，每段按住追踪键运行、松键复位，段间不重置流水线。导出为 `reverse_r63_positive_fast.csv`，上传至 `C:\Users\16143\Desktop\Xen\DML\ndi\reverse`。

reverse 要求 4/4 完整段均出现运行态机动启用，且安全、选择、响应尾部和逐轴计算契约全部为 0 违例。不要求继续提高反转速度。
