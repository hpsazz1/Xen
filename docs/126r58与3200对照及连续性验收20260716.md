# r58与3200对照及连续性验收

## 数据身份与对照条件

2026-07-16复核`C:\Users\16143\Desktop\Xen\DML\ndi`中新上传的三份jump CSV和`config.ini`。身份统一为`DML / 1fd263e9f7e5 / r58 / legacy`，`TrackerStaleTimeoutMs=100`。本轮已按计划把`move_catch_up_max_speed_cps`改为3200，使条件速度预算关闭；其余核心配置保持320裁剪、响应120 ms、基础速度3200 counts/s、积分500 ms和预测50/50/1.0。

## 3200结果与条件预算结论

三轮真实水平误差P95为67、64.5、64 px，均值65.17 px；P99为100、95.5、96 px，均值97.17 px；框内率为47.9%、63.4%、43.7%，简单均值51.67%。上一批条件4000组P95均值59.5 px，按直接比较仅改善8.7%，低于预定15%门禁；P99虽低22.5%，框内率反而低3.64个百分点。

两批并非严格同运行负载：条件4000组DML推理为91.5～93.4 FPS、NDI接收约237 FPS，本轮3200组DML推理为68.9～72.2 FPS、NDI接收约130～134 FPS。较慢控制周期使本轮同向输出步进P95升至50～61 counts，而上一批仅13～14 counts，因此不能把P99和步进差异归因于速度上限。即使运行条件明显有利于条件4000组，其P95优势仍只有8.7%；结合此前r56全局3200/4000严格A/B仅改善1.1%，以及条件预算在上一批只覆盖49/3508帧，证据已足以否决该候选，不再继续围绕4000重复采样或降低进入阈值。正式配置保持`move_catch_up_max_speed_cps=3200`。

## r58连续性验收

本轮检测发布间隔P95为31.07、30.77、31.88 ms，最大47.10、46.45、47.27 ms；三轮分别有132、145、195个间隔超过25 ms，合计472个，完整覆盖r57发生误复位的25～48 ms区间。没有任何间隔超过100 ms。

三轮方向锁下降8、7、13次，其中只有2次发生在25 ms以上间隔后，分别位于jump2的27.65 ms和jump3的27.28 ms。两处`PredictionVelocityX`仍保留为-834.8和580.1 px/s，并明确记录`PredictionMotionEvidenceSuppressed=1`，说明这是新观测触发的正常运动证据门控，不是跟踪超时清空。三轮目标检测缺失、目标框触边和100 ms后方向锁下降均为0。由此确认r58已消除原错误时钟域导致的成熟状态硬复位。

## 阶段决策

r58检测发布时钟修复通过jump连续性门禁；jump P95均值65.17 px与r56/3200基线66.88 px接近，条件4000候选未达到收益门禁并关闭。当前不再修改legacy预测、响应、积分、速度或超时参数，避免为不同采集负载继续过拟合。

下一步使用当前r58/3200配置分别采集left、right、reverse、static各1个有效试次，验证普通场景没有新增方向硬复位、输出换向或静止停发回归。该安全回归通过后，legacy冻结为安全回退版本，可继续只读shadow阶段；active仍须等待P0-6C及影子验证完成，不能因本轮jump通过而启用。
