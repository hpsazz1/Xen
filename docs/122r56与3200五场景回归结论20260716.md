# r56与3200五场景回归结论

## 数据身份

2026-07-16复核 `C:\Users\16143\Desktop\Xen\DML\ndi` 最新五场景。全部CSV为 `DML / 9c308986f0d5 / 20260716T125354Z / r56 / legacy`，运行记录确认响应120 ms、速度预算3200 counts/s；配置文件保持积分500 ms和预测50/50/1.0。

FrameID按 `reverse 0–870`、`jump 871–1949`、`static 1950–2679`、`left 2680–4041`、`right 4042–5334` 连续排列，各文件互不重叠。left/right分别切分出3轮，static切分出12段；reverse与jump各为1个连续长试次。

## 结果

| 场景 | 试次 | 真实X P95(px) | 真实X P99(px) | X稳态P95(px) | 真实Y P95(px) | 框内率 | 限速率 | 预测中断 | 输出换向 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| jump | 1 | 77.0 | 108.0 | 110.5 | 25.85 | 40.9% | 8.6% | 49 | 10 |
| left | 3 | 14.5 | 15.5 | 13.17 | 5.15 | 100% | 0% | 0 | 0 |
| right | 3 | 14.83 | 17.5 | 14.67 | 5.83 | 98.2% | 0% | 0 | 0 |
| reverse | 1 | 20.0 | 22.5 | 19.0 | 3.2 | 100% | 0% | 7 | 16 |
| static | 12 | 4.62 | 4.62 | 4.62 | 4.24 | 61.7% | 0% | 0 | 0 |

left/right六轮预测生效率分别为100%和98.6%，预测方向翻转与设备输出换向均为0，同向输出步进P95均为3 counts。reverse共16次反转并全部恢复，恢复均值63.5 ms、P95 92 ms，输出换向最大4 counts。static十二段轴向输出均为0；X/Y P95均值4.62/4.24 px，个别段停留在6 px进入半径之外但仍位于9.6 px释放半径内，属于回差保持而非复位或振荡。

jump目标速度X P95为2336.7 px/s，真实X P95 77 px、P99 108 px，末1秒P95升至110.5 px；误差不是只存在于启动期。结合上一轮3200三次独立试次的71/64.5/55 px，r56/3200共四次jump真实X P95均值为66.88 px，持续高于可接受范围。当前jump失败具有重复性。

## 结论与阶段门禁

r56/3200可冻结为普通场景安全基线：left/right、reverse和static均没有速度、预测连续性或末尾复位回归。jump仍未通过，因此不能把该版本判定为五场景完成，也不能进入active阶段。

全局4000已经由三轮A/B否决，30 ms响应也已命中大误差且不能继续缩短。下一项优化若继续推进，只允许顶层设计“条件速度预算”：基础预算保持3200，仅在真实大误差已经建立且高速证据可靠时短时开放独立上限，并在误差收敛或证据撤销时立即回到3200。实现前必须明确配置语义、进入/退出滞回、绝对上限和CSV诊断；实现后必须重新完成专项单测、DML/CUDA构建和五场景实机回归。不得直接把4000重新设为全局默认，也不得放松已验证的高速预测安全门控。
