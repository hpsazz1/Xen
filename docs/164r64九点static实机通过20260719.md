# r64 九点 static 实机通过

## 背景

r63在三轮静止目标Profile采集中偶发14帧运行态机动误启用。联合static、jump和reverse原始命令历史复放后，35 ms是首个消除新负样本失败且保留全部正样本运行段覆盖的不确定度尾部。r64只修改DML shadow门控证据的有界尾部，不改变真实相机补偿、Kalman状态、12°/s门槛、120 ms保持或legacy设备输出。

## 数据与配置

用户上传：

- `C:\Users\16143\Desktop\Xen\DML\ndi\static.csv`；
- `C:\Users\16143\Desktop\Xen\DML\ndi\config.ini`。

CSV共2421帧，身份为`DML / 6218a875ae46 / 20260719T141332Z / r64`。配置与逐帧诊断一致：shadow、20/20 ms有限响应、`maneuver_gated_ca + 8000/12/120`、不确定度增益1.25、尾部35 ms、轨迹整形关闭、Profile标定关闭。不存在混合后端、混合提交、`-dirty`或旧控制器数据。

## 排查和验证过程

正式分析器按static文件语义检查构建身份、模式降级、估计有限性、时序、暂停零命令、暂停连续观测、选择序列、有限响应和逐轴不确定度乘积。文件级结果为PASS，全部契约违例均为0。

按`AimPipelineOutputPaused`边沿切分后恰好得到9个完整运行段，帧数为116、109、156、107、114、113、111、103、128，对应时长约1.05～1.64秒。每段运行态机动启用均为0，与legacy settled重合的启用也均为0；运行段门控证据峰值依次为2.18、1.58、3.42、1.55、1.19、3.22、0.44、1.28、1.03°/s，均明显低于12°/s物理门槛。

暂停态共1364帧，295个机动启用帧全部位于松键人工换位期间；暂停态最终设备命令、发送命令和观测连续性违例均为0，因此不计入static运行门禁。单独分析static时overall提示缺少jump/reverse运行态覆盖，这是跨场景正样本门禁，不是本文件失败。

## 结论与下一步

r64的35 ms尾部已通过九点static实机负样本门禁，r63偶发回退未复现。当前仍保持`HOLD_SHADOW`，不得进入active或启动分轴主动Profile标定。

下一步保持程序和配置不变，分别采集一份r64 jump与当前最快reverse。要求每个完整运行段均存在机动启用，且所有身份、安全、时序、35 ms尾部和逐轴计算契约为0违例；随后与本轮static隔离汇总，正式分析器overall PASS后才关闭r64实机门禁。既有r63原始数据在35 ms下已经复放覆盖jump 7/7和reverse 6/6，因此本轮只做同身份运行确认，不再扫描增益、尾部或其他参数。
